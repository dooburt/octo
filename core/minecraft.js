import fs from "fs-extra";
// const os = require("os");
// const path = require("path");
// const { RCON } = require("rcon.js");

class Minecraft {
  constructor() {
    this.properties = Object.assign({}, serverProperties);
  }
}

export default Minecraft;
